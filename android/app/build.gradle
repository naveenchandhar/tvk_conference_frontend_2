plugins {
    id("com.android.application")
    id("kotlin-android")
    id("com.google.gms.google-services") // Apply the Google services plugin
    id("com.google.firebase.crashlytics") // Optional: Apply Firebase Crashlytics plugin if needed
    id("com.google.firebase.firebase-perf") // Optional: Apply Firebase Performance plugin if needed
    id("dev.flutter.flutter-gradle-plugin") // Apply Flutter plugin
}

android {
    namespace = "com.example.tvk_maanadu"
    compileSdk = 34
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.tvk_maanadu"
        minSdk = 28
        targetSdk = 34
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    // Import the Firebase BoM (Bill of Materials)
    implementation(platform("com.google.firebase:firebase-bom:33.1.2"))

    // Add the dependencies for Firebase products you want to use
    // For example, Firebase Analytics
    implementation("com.google.firebase:firebase-analytics")

    // Add dependencies for other Firebase products
    // e.g., Firebase Crashlytics, Performance Monitoring, etc.
    // implementation("com.google.firebase:firebase-crashlytics")
    // implementation("com.google.firebase:firebase-perf")
}
